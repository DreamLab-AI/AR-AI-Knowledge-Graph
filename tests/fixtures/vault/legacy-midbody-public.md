# Legacy Mid-Body Public

EXP-V03 (negative): the marker appears only AFTER the first heading, and again
inside a code fence. Neither position counts.

public:: true

To publish a page you would write:

```
public:: true
```
