---
public: false
source-domain: mv
---

# Obsidian Private

EXP-V01: `public: false` and no `owl-class`. This page must NOT ingest.

The body deliberately quotes the legacy marker to prove the gate no longer
scans past the carrier:

```
public:: true
```
