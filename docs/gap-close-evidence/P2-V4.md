# P2 — V4 voice docs honesty (deprecated voice-to-swarm path corrected)

- **Item:** V4 (PRD-023 WP-10). Documentation-only; no canary (not loop-closing).
- **Repo:** VisionClaw (`gap-close/2026-07`, base `b6c1c43b`).
- **Verified:** 2026-07-08T15:57Z.
- **Maturity / tier:** `canon/practice mismatch` → `integrated` (docs match
  shipped behaviour).

## The proven gap (recon, carried forward)

PRD-023 WP-10 records V4 as a `canon→practice` mismatch: `voice-integration.md`
and `voice-routing.md` present a **voice-to-swarm command path as live** with no
deprecation notice, while the shipped React client runs the PTT-to-selected-actor
governed loop (`is_swarm_command` → `handle_swarm_voice_command` returns *"Swarm
voice commands deprecated - use API endpoints instead"*,
`src/handlers/speech_socket_handler.rs`). The single load-bearing unqualified
claim was `voice-integration.md:11`: *"Voice Commands: Natural language commands
for swarm orchestration"*.

## The fix (both docs, honesty standard of the REC-2 doc work)

Same discipline as REC-2: sweep for every sibling assertion, then correct with a
status banner rather than deleting the legacy design.

### `docs/how-to/features/voice-integration.md`
- Top-of-file **status banner** (WP-10 AC2): the shipped React path is
  push-to-talk-to-selected-actor (signed 31402 → `/v1/voice-intent` → Kokoro
  ack, COM-15/V1), with the V3 confidence gate; the voice-to-swarm-orchestration
  path is **deprecated in the React client** and the diagrams below describe the
  legacy design and the separate Godot native client; unbound commands reach the
  settings assistant, not swarm orchestration.
- Line 11 bullet corrected from "swarm orchestration" to the governed PTT path.
- Sibling assertions annotated: the "Voice Commands" request example, the "Voice
  Command Processing" flowchart, and the "Voice Command Flow" sequence diagram
  each carry a "Deprecated / Legacy design (V4)" note pointing at the banner.

### `docs/how-to/features/voice-routing.md`
- Top-of-file **status banner**: Plane 1 "Private Agent Commands" in the React
  client is the governed PTT-to-selected-actor loop; the "Agent Command Parser"
  that drove swarm orchestration directly is deprecated in the React client; the
  parser box in the diagrams denotes that legacy / Godot-native design.
- The overview line ("PTT controls audio routing between agent commands…")
  clarified to name the governed loop.

## Falsification → met

*Falsified if either voice doc still presents the voice-to-swarm path as live in
the React client without a status banner.* — **Met**: the single unqualified
live claim (line 11) is corrected; every remaining "voice-to-swarm" mention sits
inside a banner / deprecation / legacy note; both docs carry a top-of-file V4
status banner.

## Receipts

```
$ git rev-parse HEAD
b6c1c43b59280eb94392dba23cd51c9df347f16e
$ date -u '+%Y-%m-%dT%H:%M:%SZ'
2026-07-08T15:57:39Z

# GREP 1 — BEFORE (git HEAD): the unqualified voice-to-swarm-as-live claims
$ git show HEAD:docs/how-to/features/voice-integration.md | grep -niE \
    "natural language commands for swarm|commands to control the swarm"
11:- **Voice Commands**: Natural language commands for swarm orchestration
202:Send natural language commands to control the swarm.
$ git show HEAD:docs/how-to/features/voice-routing.md | grep -niE \
    "controls audio routing between agent commands"
20:… Push-to-talk (PTT) controls audio routing between agent commands and spatial voice chat.

# GREP 1 — AFTER: line 11 corrected to the governed PTT path
$ grep -nE "Voice Commands\*\*:" docs/how-to/features/voice-integration.md
31:- **Voice Commands**: Push-to-talk commands addressed to a selected agent
   (governed kind-31402 → /v1/voice-intent, COM-15/V1). The legacy
   voice-to-swarm-orchestration parser is deprecated in the React client — see
   the status banner above.

# GREP 2 — AFTER: both docs carry a V4 status banner (sibling-assertion sweep)
$ grep -cE "Status \(2026-07, PRD-023 WP-10 / V4\)|Deprecated in the React client|Legacy design \(V4\)" \
    docs/how-to/features/voice-integration.md docs/how-to/features/voice-routing.md
docs/how-to/features/voice-integration.md:4
docs/how-to/features/voice-routing.md:1
```

An adversarial verifier re-running GREP 1 against the working tree finds no
unqualified voice-to-swarm-as-live claim; every occurrence is within a banner or
a "Deprecated / Legacy design (V4)" note.
