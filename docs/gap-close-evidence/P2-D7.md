# P2 — D7 pre-action intent legibility (declared intent)

- **Item:** D7 (register position — pre-action intent legibility). No canary:
  D7 is a canon-position/reconciliation item, not loop-closing.
- **Repo:** VisionClaw (`gap-close/2026-07`, base `b6c1c43b`).
- **Verified:** 2026-07-08T15:57Z.
- **Maturity / tier:** the affordance is `integrated` on the VisionClaw side
  (envelope field + panel display, unit-tested). Live population is
  `pending-live-session` — it becomes visible only when the producer (agentbox)
  declares an `intent` on the envelope.

## Scope statement (honest, as the task requires)

**PRD-023 does NOT carve D7 into a work package.** D7 is a **canon-position**
item: the canon owns the intent-legibility *position*
(`VisionFlow/docs/PRD-gap-close-sprint.md:60` — "theory→canon", owned by
VisionClaw, P2; `PRD-gap-close-canon.md:29` — "D7 intent-legibility position |
canon reconciliation … VisionClaw D7 cites"). VisionClaw *cites* the canon
position and carries the affordance. This closure implements exactly the minimal
affordance the task scopes: **an additive `intent` field in the agent-events
envelope + a "about to: <declared action>" display in the P1 `AgentDetailPanel`
steering surface when present.** No broader D7 mechanism is claimed.

## The fix

### Server — `src/agent_events/schema.rs`
- `AgentActionEnvelope` gains `intent: Option<String>`, additive and versioned
  exactly like the WP-7 CTC fields: `#[serde(default, alias = "declared_intent")]`
  so a producer that declares no intent still deserialises to `None`, both the
  register spelling `intent` and the fuller `declared_intent` parse, and the
  identity-blind binary `0x23` projection is unaffected (intent is legibility
  metadata, not a GPU field).
- Accessor `declared_intent(&self) -> Option<&str>`.
- The two `#[cfg(test)]` envelope literals (`agent_beam_actor.rs`,
  `provenance.rs`) carry the new `intent: None`.

### Client — `AgentDetailPanel` display
- `BotsAgent` gains `declaredIntent?: string`
  (`client/src/features/bots/types/BotsTypes.ts`).
- `BotsDataContext` maps it from node metadata
  (`declared_intent` / `intent`) — the join from the envelope field to the panel.
- `AgentDetailPanel.tsx` renders a distinct amber "Declared Intent — About to:
  <declared action>" block **above** the past-activity "Current Task", shown only
  when `declaredIntent` is present (never fabricated).

## Falsification → met

The task's honesty bar: implement the minimal affordance PRD-023 scopes (or state
that D7 is canon-position-only). — **Met and stated**: D7 is canon-position; the
concrete affordance (additive envelope `intent` + panel "about to" display) is
implemented and unit-tested on both sides; live population honestly labelled
`pending-live-session` (the producer is agentbox).

## Receipts

```
$ git rev-parse HEAD
b6c1c43b59280eb94392dba23cd51c9df347f16e
$ date -u '+%Y-%m-%dT%H:%M:%SZ'
2026-07-08T15:57:39Z

# R1 — envelope intent field: parses, alias parses, absent → None
$ cargo test --lib declared_intent_deserialises_and_is_optional
test agent_events::schema::tests::declared_intent_deserialises_and_is_optional ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 793 filtered out

# R2 — panel shows "About to: <declared action>" when present, nothing when absent
$ npx vitest run src/features/bots/components/__tests__/AgentDetailPanel.intent.test.tsx
 ✓ AgentDetailPanel.intent.test.tsx (2 tests)
 Test Files  1 passed (1) · Tests  2 passed (2)
```

An adversarial verifier re-running R1 confirms both spellings deserialise and an
envelope with no intent decodes to `None`; R2 confirms the panel renders the
declared intent only when present and shows no "About to" line otherwise.
