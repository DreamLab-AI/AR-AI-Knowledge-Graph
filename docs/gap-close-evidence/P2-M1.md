# P2 — M1: in-headset identity badge (verified did:nostr render)

- **Item:** M1 (headset identity) — PRD-023 WP-9, ADR-130 Decision 1 / Decision 6
- **Canary:** `CANARY-VC-M1-HUD` (standing, P0) — "Godot avatar renders a verified DID badge in an xr-runtime session"
- **Base SHA:** `57b32faeeeac9019d9d645111aa2b3cd8beb45d8` (branch `gap-close/2026-07-xr`); the render lands in this Stage-2 commit.
- **Verified:** 2026-07-08T15:58Z
- **Maturity:** `scaffolded` → **`integrated` (render), verify-flag `scaffolded`-honest**. The badge now renders the did:nostr the presence join carries (`graph_scene.gd:431` `set_meta("did", did)`), with an unverified claim held visually distinct per ADR-130 D6. The badge reads **verified** only when the client sets the verified flag; the on-device Schnorr-challenge verification that flips it, and the live sidecar render that fires `CANARY-VC-M1-HUD`, are **pending-live-session** (no godot binary in this container).

## Falsification (PRD-023 WP-9, M1 clause)

*"WP-9 is falsified if the Godot HUD still carries no identity … control while `graph_scene.gd:431` holds the DID …"* — before: `avatar.gd` wrote only the self-reported `display_name` to the nameplate and the DID sat unused in metadata. After: the DID is rendered on a dedicated badge with a verified/unverified visual, sourced from the same meta.

Invariant 2 (DDD): *identity is verified before trust.* The badge defaults to **unverified** (amber, "?") for a presence peer — the server-vouched roster DID is **not** client-trusted on presentation. It reads verified only when `set_verified(true)` is called after the Schnorr check. No badge fabricates a "verified" state.

## What landed

### 1. Human-peer avatar (`scenes/Avatar.tscn`, `scripts/avatar.gd`)
- New `Head/DidBadge` billboarded `Label3D` below the nameplate.
- `set_identity(display_name, did, verified)` — sets the nameplate and the DID badge (short-DID `…last8` of the pubkey hex). `set_verified(bool)` flips the state.
- Verified → check mark + `Color(0.75,1.0,0.82)`; unverified → "?" + `Color(1.0,0.72,0.28)` (amber). An empty DID renders the literal `unverified` — an honest absence, never a fabricated id.
- The badge drops with the nameplate at LOD Medium (`_set_label_visibility`), so distance never leaves a floating label.
- `graph_scene.gd:_on_avatar_joined` now calls `set_identity(display_name, did, false)` — the DID that reached the meta is rendered, unverified until the client checks it.

### 2. Agent avatar (`scenes/AgentAvatar.tscn`, `scripts/agent_avatar.gd`)
- The geometric agent embodiment carries the same badge semantics (`set_identity` / `set_verified`, `_short_did` `…last8`, verified/unverified colour), pooled as the avatar's own `Label3D`.

## Receipts

```
$ git rev-parse HEAD          # base
57b32faeeeac9019d9d645111aa2b3cd8beb45d8
$ date -u '+%Y-%m-%dT%H:%M:%SZ'
2026-07-08T15:58:04Z
```

- **Scene-tree dump** (`Avatar.tscn`) shows the new node under `Head`:
  ```
  Avatar [Node3D]
    Head [MeshInstance3D]
      Nameplate [Label3D]
      DidBadge [Label3D]      ← new (M1)
      VoiceIndicator [MeshInstance3D]
      VoicePlayer [AudioStreamPlayer3D]
    LeftHand / RightHand [MeshInstance3D]
  ```
- **GUT test files** (headless-pending; no godot binary here): `tests/unit/test_did_badge.gd` (nameplate + DID badge populated; verified colour; unverified distinct; `set_verified` flip; badge drops at Medium LOD; `_short_did` `…last8`), plus the agent badge in `tests/unit/test_agent_avatar.gd`. `tests/unit/test_scene_load.gd` asserts `Head/DidBadge` exists.
- **Static review:** tab-indented, brackets balanced (Python static pass, all files OK).

## Honest residual

- **The verified flag is set by the client only after the Schnorr check runs** (ADR-130 D6). That check reuses the `signer.rs` / server `nostr_identity_verifier` path; the client-side flip on a live challenge is stage-3/on-device. Today every rendered badge reads **unverified** — the honest floor, never over-claimed.
- **`CANARY-VC-M1-HUD` fire is pending-live-session** in the agentbox/xr-runtime Monado sidecar (VNC :5904). The render is built and unit-specified; the on-device fire is not claimed.

---

## Stage 3 — reconciliation, register truth (2026-07-08T16:17Z)

**Base SHA:** `0f3a1b60c0cc00ffdc8fcc7839066e8e37d808d0` (branch `gap-close/2026-07-xr`).

### Maturity — register truth
**Tier: `standalone`** (reconciled down from the Stage-2 "integrated (render)" wording). Per ADR-130 Decision 6 + Decision 4 discipline (*"A closure that claims a live MR session must carry a sidecar receipt, not a static read"*) and the ADR-002 seven-tier vocabulary: the DID badge render + verified/unverified visual + the `signer.rs` Schnorr path are **real and unit-specified** (code+test-proven), but the in-headset render and the client-side verified-flip on a live challenge are **runtime-unproven here** (no `godot` binary; the Schnorr verify against a live agentbox challenge is the on-device step). That is exactly `standalone`, not `integrated`. `integrated` is one **`CANARY-VC-M1-HUD` sidecar fire** away.

### Acceptance criteria — code+test-proven vs pending-live-session
| AC (WP-9, M1) | State | Evidence |
|---|---|---|
| `Avatar.tscn`/`avatar.gd` render a `did:nostr`/pubkey-suffix + verification badge from the `graph_scene.gd:431` meta | render instantiated + unit-specified | `Head/DidBadge` `Label3D`, `set_identity`/`set_verified`, `_short_did …last8`; GUT `test_did_badge.gd`, `test_scene_load.gd`. On-device render **pending-live-session**. |
| Verified by the `signer.rs` Schnorr path before the badge reads verified | code-proven (verifier) / flip pending | `signer.rs` `nip98`/challenge path — `signature_verifies_against_server_message_construction`, `nip98_event_is_well_formed_and_signature_verifies`. Live-challenge flip is on-device; every rendered badge reads **unverified** here (honest floor). |

### Canary
`CANARY-VC-M1-HUD` (standing, P0) — **seeded** (P0 wave per the PRD-023 canary table; `P0_CANARIES` in `liveness_harness.rs`), **armed, unfired**. Fires on the sidecar in-headset verified-badge render. Pending-live-session.

### Falsification (re-checked)
*"WP-9 is falsified if the Godot HUD still carries no identity control while `graph_scene.gd:431` holds the DID."* — not falsified: the DID that reaches the meta is rendered on a dedicated badge with a verified/unverified visual; the badge defaults to **unverified** (no fabricated "verified" state). Full-suite receipts: see `P2-M3.md` Stage 3 (194 gdext + 82 presence tests green; host `.so` built; NDK unavailable/recorded).

---

> **2026-08-22 runtime note.** On-device render of the XR client was achieved on
> the VIVE Pro (branch `xr-vive-runtime`, Godot 4.6.1 Compatibility + NVIDIA 580
> + SteamVR) — see `P2-vive-closeout-2026-08-20.md` §7. The `CANARY-VC-M1-HUD`
> fire (in-headset verified DID badge) is **still pending** — tier unchanged
> (not promoted to `integrated`).
