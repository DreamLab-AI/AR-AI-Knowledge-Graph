# P2 — M4: gaze + targeting resolving a real selection (Godot copresence, Stage 1 Rust core)

- **Item:** M4 (targeting) — PRD-023 WP-9, ADR-130 Decision 4
- **Canary:** `CANARY-VC-M4-RAY` (one-shot, P2) — "Controller or gaze ray resolves a non-origin agent-node selection"
- **Base SHA:** `453bd41b12b5bba4ccf83762e1eed6b2a4bacbd1` (branch `gap-close/2026-07-xr`, a child of `gap-close/2026-07`)
- **Verified:** 2026-07-08T15:27Z
- **Maturity:** `scaffolded` → **`integrated` (Rust targeting mechanic)**. The controller-ray + pinch + head-gaze-dwell arbiter resolves a real non-origin agent-node selection with the target's `did:nostr` carried, all unit-proven headless. The live xr-runtime sidecar run that fires `CANARY-VC-M4-RAY` is **pending-live-session** — honestly labelled, not claimed closed (no `godot` binary in this container; PRD-023 WP-9 verification path).

## Falsification (PRD-023 WP-9, M4 clause)

*"WP-9 is falsified if … the targeting ray casts from world origin in a live sidecar session … or if M3 is claimed above `scaffolded` without a proxemics solver and a gaze cue instantiated …"*
M4 acceptance AC3: *"a controller ray resolves a real agent-node selection (non-origin) live in the xr-runtime sidecar, and a head-gaze fallback resolves a selection when both controllers are untracked."*

## What landed (`xr-client/rust/src/`)

### 1. `gaze.rs` — unified head/eye gaze ray with one-euro smoothing
- `GazeSource::{Head, Eye}` behind one `GazeRay { origin, dir, source }`. **Head-gaze primary**; eye-gaze is a progressive enhancement gated on a runtime flag: `GazeResolver::set_eye_gaze_supported(bool)` (fed from `OpenXRInterface.is_eye_gaze_interaction_supported()`, called only after OpenXR init). `effective_source(Eye)` degrades to `Head` unless supported, so the Quest-3 floor device never selects eye-gaze.
- `OneEuroFilter` (Casiez et al. 2012) implemented from scratch: adaptive cutoff `min_cutoff + beta·|edx|`, `alpha = 1/(1+tau/dt)`. First sample passes through; non-positive `dt` holds the last value (no divide-by-zero on shared timestamps).
- Property/convergence tests: `converges_to_constant_input` (500 constant samples → within 1e-3), `output_never_overshoots_input_range` (low-pass output stays inside the running [min,max] — no ringing), `smoothing_reduces_direction_jitter` (angular error to the mean is strictly lower than the raw samples'), `zero_dt_holds_last_value`, `resolved_dir_is_unit_length`.

### 2. `selection.rs` — three resolvers → one arbiter → one `SelectionEvent`
- `SelectionEvent { target_entity: u32, did_nostr: Option<String>, resolver, timestamp_us }`.
- **Resolver 1 (controller ray)** and **Resolver 2 (pinch)** both feed the *existing* `interaction.rs` path: `find_target` for the corridor test, `is_grab_active` for pinch. Explicit selection fires on the **rising edge** of the trigger (`ControllerRay`) or of the pinch (`Pinch`) — a held trigger does not repeat (`trigger_hold_does_not_repeat`).
- **Resolver 3 (gaze dwell)**: `DwellCharger` charges a target over a **600 ms default within a configurable 400–800 ms band**, with a **target-size floor** (`target_radius_floor_m`, small nodes get a minimum acquisition radius), **activation hysteresis** (a charging target is kept while within `hysteresis_factor×` its radius), and **cancel-on-saccade** (charge resets when gaze angular velocity exceeds `saccade_cancel_rad_s`). `charge_ratio()` drives the radial reticle fill (Midas-touch mitigation from the brief). It fires once per dwell (`fired_latch`).
- **Arbitration priority**: explicit trigger/pinch **beats** dwell (`explicit_click_beats_dwell` — the dwell resets when an explicit pick lands). Dwell **only arms** when no controller is tracked (`dwell_fires_when_no_controller`) **or** `hands_free` is set (`dwell_arms_in_hands_free_even_with_controller`); a tracked controller with no hands-free flag disables the charger entirely (`dwell_disabled_while_controller_tracked`).
- **Non-origin selection**: the ray origin is the caller-supplied controller/gaze origin (never a hard-coded `[0,0,0]`); candidates are real node positions. `controller_trigger_click_selects_and_carries_did` resolves node 7 at `[0,0,-5]` with its DID.

### 3. `did:nostr` carry + the named server-side integration point
- `SelectionArbiter::register_identity(node_id, did_nostr)` fills a `HashMap<u32,String>`; a resolved event's `did_nostr = registry.get(target)` (`None` when the entity has no DID yet — an honest absence, `no_did_yields_none_did_field`).
- The 0x03 graph binary record (fixed 52 bytes) has no room for a 32-byte pubkey, so identity rides the `initialGraphLoad` node metadata instead. **Additive extension landed HERE**: `binary_protocol::parse_agent_identities(text)` reads an optional `did_nostr`/`didNostr` per node (id masked with `NODE_ID_MASK`) and returns `(node_id, did)` pairs (`parse_agent_identities_extracts_did_and_masks_flags`).
  - **Named integration point (server-side emit):** the VisionClaw graph server must include `did_nostr` on each agent node in the `initialGraphLoad` payload, sourced from the agent record's `did_nostr` field landed by COM-14 P0 (`src/services/bots_client.rs` / `agent_visualization_protocol.rs`). Until it does, `parse_agent_identities` returns empty and selections carry `None` — no fabricated identity. GDScript wires the parser output into `SelectionArbiterNode::register_identity` (Stage 2 glue).

### 4. GDScript-facing nodes (compile-verified, `cfg(not(test))`)
`GazeTracker` (resolve / set_eye_gaze_supported), `SelectionArbiterNode` (begin_frame / push_controller / set_candidates / set_gaze / tick → `selection_made(node_id, did_nostr, resolver)` / charge_ratio). Auto-registered via the `#[gdextension]` `ExtensionLibrary`.

## Receipts

```
$ git rev-parse HEAD
453bd41b12b5bba4ccf83762e1eed6b2a4bacbd1
$ date -u '+%Y-%m-%dT%H:%M:%SZ'
2026-07-08T15:27:33Z

$ cargo test -p visionclaw-xr-gdext --lib   # (from xr-client/rust)
test gaze::tests::converges_to_constant_input ... ok
test gaze::tests::output_never_overshoots_input_range ... ok
test gaze::tests::smoothing_reduces_direction_jitter ... ok
test gaze::tests::eye_gaze_degrades_to_head_when_unsupported ... ok
test selection::tests::controller_trigger_click_selects_and_carries_did ... ok
test selection::tests::pinch_rising_edge_selects_via_pinch_resolver ... ok
test selection::tests::dwell_fires_when_no_controller ... ok
test selection::tests::dwell_disabled_while_controller_tracked ... ok
test selection::tests::dwell_cancels_on_saccade ... ok
test selection::tests::explicit_click_beats_dwell ... ok
test binary_protocol::tests::parse_agent_identities_extracts_did_and_masks_flags ... ok
test result: ok. 136 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out

$ cargo build   # (from xr-client/rust) — proves the cfg(not(test)) Godot wrappers compile
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 12.75s
```

## Honest residual

- **Live sidecar run is pending.** `CANARY-VC-M4-RAY` is a one-shot that fires when a controller or gaze ray resolves a non-origin agent-node selection *in the agentbox/xr-runtime Monado sidecar* (VNC :5904). No `godot` binary runs in this container; the mechanic is proven headless (136 tests), the on-device fire is **pending-live-session**.
- **The server-side `did_nostr` emit on `initialGraphLoad` is not yet shipped** — it is the named integration point above. The client parses it additively today; the parser returns empty (and DIDs read `None`) until the server includes the field.
- Stage-2 GDScript glue (feeding `XRController3D.global_position`, `XRCamera3D` forward, and the parsed identities into the arbiter node each frame) is receipted separately; this file covers the Rust targeting core.

---

## Stage 2 addendum — the scene glue (2026-07-08T15:58Z)

The Stage-1 targeting core is now **wired into the running scene**. `CANARY-VC-M4-RAY` (one-shot, the non-origin selection fire) stays **pending-live-session**; the arbiter is fed real controller rays, a smoothed head-gaze ray, and real agent-candidate positions each frame.

### What landed (Stage 2, `scripts/graph_scene.gd`)
- **Node construction** — `GazeTracker.create()`, `SelectionArbiterNode.create()`, `ProxemicsSolver.create()`, `NostrAuth.create(secret)` in `_ready`; `selection_made` connected to `_on_selection_made`.
- **`_update_selection`** — per frame: `begin_frame`; `push_controller` for each tracked `XRController3D` (origin = `global_position`, dir = `-basis.z`, trigger float, `trigger_down` on the 0.7 activation threshold); `set_gaze` from the `GazeTracker.resolve()`-smoothed camera ray; `set_candidates` from the live agent positions (handle ids, core radius + margin); `tick(hands_free, now_us, dt_us)`. Dwell arms only when no controller is tracked (`hands_free`), matching the Stage-1 arbitration.
- **Non-origin origins** — the ray/gaze origins are the real controller/camera positions, never `[0,0,0]` (the WebXR defect D1 retired). Candidates are real agent `global_position`s.
- **`charge_ratio` → HUD reticle** — `hud.set_dwell_charge(_selection.charge_ratio())` drives the charging radial (Midas-touch mitigation).
- **Eye-gaze probe (#113717 guard)** — `_probe_eye_gaze` queries `is_eye_gaze_interaction_supported()` (behind `has_method`, after OpenXR init) and feeds `GazeTracker.set_eye_gaze_supported`; head-gaze stays primary on Quest 3.
- **String↔u32 bridge** — agents carry an integer `handle` (≥1) registered with `register_identity(handle, did)`; `_on_selection_made` maps the handle back to the agent id and, for an awaiting-approval agent, opens the M2 panel.

### Stage-2 receipts
- **Scene-tree dump** (`GraphScene.tscn`): `XROrigin3D{XRCamera3D, LeftController, RightController}`, `AgentSpawner`, and the `HUD` instance carrying the reticle.
- **GUT test files** (headless-pending): `tests/unit/test_graph_agents.gd` (`_on_selection_made` on an agent handle with an open case opens the intervention panel — the selection→governance wire), `tests/unit/test_xr_config.gd` (eye-gaze probed, never blindly enabled).
- Rust core unchanged; the 136→142 `cargo test` delta is the Stage-2 `lod`/`signer` additions, all green.

### Stage-2 honest residual
- **`CANARY-VC-M4-RAY` is pending-live-session** — it fires when a controller or gaze ray resolves a non-origin agent-node selection in the xr-runtime sidecar. The wire is built end to end scene-side; the on-device fire is not claimed.
- **The server-side `did_nostr` emit on `initialGraphLoad`** (Stage-1 named integration point) still gates graph-node identity carry; agent avatars carry the DID directly from `spawn_agent`.

---

## Stage 3 — reconciliation, register truth (2026-07-08T16:17Z)

**Base SHA:** `0f3a1b60c0cc00ffdc8fcc7839066e8e37d808d0` (branch `gap-close/2026-07-xr`).

### Maturity — register truth
**Tier: `standalone`** (reconciled down from the Stage-2 "integrated (Rust targeting mechanic)" wording). The controller-ray + pinch + head-gaze-dwell arbiter resolves a real **non-origin** agent-node selection carrying the target `did:nostr`, all **unit-proven headless**, and is fed real controller/camera rays + real candidate positions each frame scene-side. But the **live selection in the xr-runtime sidecar** (the `CANARY-VC-M4-RAY` predicate) is **runtime-unproven here** — `standalone`, not `integrated`. `integrated` is one sidecar ray-resolution fire away.

### Acceptance criteria — code+test-proven vs pending-live-session
| AC (WP-9, M4) | State | Evidence |
|---|---|---|
| Controller ray resolves a real (non-origin) agent-node selection live in the sidecar | **code+test-proven headless** / live sidecar pending | `selection.rs` `controller_trigger_click_selects_and_carries_did` (node 7 at `[0,0,-5]`, ray origin caller-supplied, never `[0,0,0]`); `graph_scene._update_selection` feeds `XRController3D.global_position`. Sidecar fire **pending-live-session**. |
| Head-gaze fallback resolves a selection when both controllers untracked | **code+test-proven** | `selection.rs` `dwell_fires_when_no_controller`, `dwell_disabled_while_controller_tracked`, `dwell_cancels_on_saccade`, `explicit_click_beats_dwell`; `gaze.rs` one-euro smoothing (`converges_to_constant_input`, `smoothing_reduces_direction_jitter`); eye-gaze gated on `is_eye_gaze_interaction_supported()` (`eye_gaze_degrades_to_head_when_unsupported`). |

### Canary
`CANARY-VC-M4-RAY` (one-shot, P2) — **now seeded** in `liveness_harness.rs` `P2_CANARIES` (new this stage), registered at boot via `seed_p2_canaries()`. **Armed, unfired**; fires when a controller or gaze ray resolves a non-origin agent-node selection in the sidecar. Seed proven by `tests/liveness_harness_test.rs::p2_seed_registers_the_mr_copresence_canaries_armed`.

### Falsification (re-checked)
*"WP-9 is falsified if … the targeting ray casts from world origin in a live sidecar session."* — not falsified: ray/gaze origins are the real controller/camera positions (never `[0,0,0]`), asserted by `controller_trigger_click_selects_and_carries_did` and the scene glue. The WebXR world-origin defect (D1) is retired, not reproduced. Full-suite receipts: see `P2-M3.md` Stage 3.
